<div class="page-width">
<h2 style="text-align: left">{{section.settings.mein_erstes_textfeld}}</h2>
  
<div class="fl-container">
{% for block in section.blocks %}  
  <div class="fl-item">
  <img src="{{block.settings.image | img_url: '2048x'}}" style="width: 300px;" alt="Trust-Image">
  <p class="fl-trust-text">{{block.settings.beschreibungstext}}</p>
  </div>
{% endfor %}
</div>
</div>

{% schema %}
  {
    "name": "Erste eigene Section",
    "max_blocks": 5,
    "settings": [{
      "type": "text",
      "id": "mein_erstes_textfeld",
      "label": "Überschrift",
      "default": "Hey, ich bin deine erste Überschrift"
}],
  "blocks": [
     {
       "name": "Bild-Text",
       "type": "custom",
       "settings": [
         {
           "type": "image_picker",
           "id": "image",
           "label": "Image"
         },
         {
           "type": "text",
           "id": "beschreibungstext",
           "label": "Beschreibungstext",
           "default": "Dies ist ein Beschreibungstext"
         }
       ]
     }
   ],
    "presets": [
      {
        "name": "Erste eigene Section - Presets Titel",
        "category": "Custom"
      }
    ]
  }
{% endschema %}

<style>
  .fl-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: top;
    }
    
    .fl-item {
      /*background-color: lightgrey;*/
      width: 19%;
    }
    
    .fl-item p, .fl-item img {
      text-align: center;
      margin: auto;
      display: block;
    }
    
    .fl-item p {
      padding: 8px;
      margin-top: 16px;
      margin-bottom: 16px;
    }
    
    .fl-item img {
      margin-top: 32px;
      width: 44px;
    }
    
    @media only screen and (max-width: 600px) {
      .fl-item {
        background-color: white;
        width: 49%;
      }
      .fl-item:first-child {
        display: none;
      }
    }
    
    .none {
      display: none;
    }
</style>

{% stylesheet %}
{% endstylesheet %}
    
{% javascript %}
{% endjavascript %}